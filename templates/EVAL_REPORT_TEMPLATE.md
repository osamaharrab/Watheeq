# Evaluation report

Generated by `python scripts/run_eval.py` after a local stack run.

The generated report must include the question checksum, generation time, all 48 question IDs, taxonomy classes, per-class outcome counts, citation-validity metrics, unsupported-assertion count, failures, and the ownership-slice/full-registry limitation. It must not contain invented metrics before the harness runs.
